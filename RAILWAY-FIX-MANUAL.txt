🚨 RAILWAY DEPLOYMENT FIX - MANUAL STEPS
===========================================

❌ PROBLEM: "Application failed to respond" 
✅ SOLUTION: Set environment variables in Railway

🔧 IMMEDIATE ACTION REQUIRED:

1. 🚂 Go to Railway Dashboard:
   https://railway.app/dashboard

2. 📁 Click on your "ideaapp-new" project

3. ⚙️ Click "Variables" tab (should be visible in the project view)

4. ➕ Click "Add Variable" and add these ONE BY ONE:

   ┌─────────────────────────────────────────────────────┐
   │ Variable Name: DATABASE_URL                         │
   │ Value: mongodb+srv://adrika_new:adrikanew@cluster0.mb2ligy.mongodb.net/hackideas?retryWrites=true&w=majority&appName=Cluster0 │
   └─────────────────────────────────────────────────────┘

   ┌─────────────────────────────────────────────────────┐
   │ Variable Name: NODE_ENV                             │
   │ Value: production                                   │
   └─────────────────────────────────────────────────────┘

   ┌─────────────────────────────────────────────────────┐
   │ Variable Name: PORT                                 │
   │ Value: 5000                                         │
   └─────────────────────────────────────────────────────┘

   ┌─────────────────────────────────────────────────────┐
   │ Variable Name: JWT_SECRET                           │
   │ Value: hackideas-super-secret-jwt-key-2025          │
   └─────────────────────────────────────────────────────┘

   ┌─────────────────────────────────────────────────────┐
   │ Variable Name: JWT_REFRESH_SECRET                   │
   │ Value: hackideas-super-secret-refresh-key-2025      │
   └─────────────────────────────────────────────────────┘

   ┌─────────────────────────────────────────────────────┐
   │ Variable Name: CLIENT_URL                           │
   │ Value: https://ideaapp-new.vercel.app               │
   └─────────────────────────────────────────────────────┘

   ┌─────────────────────────────────────────────────────┐
   │ Variable Name: SERVER_URL                           │
   │ Value: https://ideaapp-new-production.up.railway.app │
   └─────────────────────────────────────────────────────┘

   ┌─────────────────────────────────────────────────────┐
   │ Variable Name: CORS_ORIGIN                          │
   │ Value: https://ideaapp-new.vercel.app,http://localhost:3000,http://localhost:5173 │
   └─────────────────────────────────────────────────────┘

5. 🚀 AFTER ADDING ALL 8 VARIABLES:
   - Railway will automatically redeploy
   - Wait 3-5 minutes for deployment to complete
   - You'll see deployment activity in Railway dashboard

6. 🧪 TEST THE FIX:
   - Visit: https://ideaapp-new-production.up.railway.app/health
   - Should return: {"status":"ok","timestamp":"...","environment":"production"}

7. 🌐 IF HEALTH CHECK WORKS, TEST YOUR APP:
   - Visit: https://ideaapp-new.vercel.app
   - Try registration and login

🆘 TROUBLESHOOTING:
- Make sure you're in the correct Railway project
- Double-check each variable name and value (copy-paste exactly)
- If still failing, check Railway logs for specific error messages
- Ensure all 8 variables are present (not 7, not 9)

⚡ WHY THIS FIXES IT:
Your server needs these environment variables to:
- Connect to MongoDB database (DATABASE_URL)
- Configure security (JWT secrets)
- Set up CORS for your frontend (CORS_ORIGIN)
- Know what environment it's running in (NODE_ENV)

Without these variables, the server crashes immediately on startup,
causing the "Application failed to respond" error.

💡 The fix is simple: just add the 8 environment variables to Railway!
